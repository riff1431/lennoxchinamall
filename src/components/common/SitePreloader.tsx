"use client";

export interface SitePreloaderProps {
  storeName?: string;
  logoUrl?: string;
}

/**
 * SitePreloader removed per user request.
 * Renders null for instant storefront loading.
 */
export function SitePreloader(_props?: SitePreloaderProps) {
  return null;
}
